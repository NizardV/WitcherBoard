/**
 * API configuration.
 *
 * The backend is assumed to run locally.
 * Keep this as a single source of truth to avoid hardcoding URLs across pages.
 *
 * If you ever need to switch environments (different port/host),
 * updating this constant is enough for the whole app.
 */
export const API_BASE = "http://localhost:3000/api";
